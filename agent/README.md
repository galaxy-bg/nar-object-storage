# kdx-agent

Privileged host-side FastAPI service for controlled appliance operations.

The agent is not an AI agent. It is the local root-level service used by the wizard to perform host operations safely.

Default bind address:

```text
127.0.0.1:7070
```
