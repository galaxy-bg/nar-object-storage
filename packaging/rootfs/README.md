# Rootfs Staging Layout

This directory models the target appliance filesystem layout.

It is not the runtime install itself. Use `scripts/install-lab.sh` to copy source files into a real or staged target root.
