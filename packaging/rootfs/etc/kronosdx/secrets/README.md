# /etc/kronosdx/secrets

Runtime secrets live here.

Expected files:

- `agent.token`
- future service-specific secret files

All files in this directory should be owned by `root:root` and set to mode `0600`.
